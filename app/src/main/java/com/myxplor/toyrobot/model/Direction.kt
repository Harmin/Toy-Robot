package com.myxplor.toyrobot.model

//Assuming directions to be direction of compass where
// North to be at top, east towards right, south bottom and west towards left
enum class Direction(val value:Int){
    NORTH(0),
    EAST(1),
    SOUTH(2),
    WEST(3)
}